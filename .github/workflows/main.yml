name: Cache Test

on:
  push:
  workflow_dispatch:

jobs:
  cache-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Cache dependencies
        id: cache-step
        uses: actions/cache@v4
        with:
          path: ~/.cache
          key: cache-5174db2

      - name: prime-cache-5174db2
        run: echo "Cache hit: ${{ steps.cache-step.outputs.cache-hit }}"
